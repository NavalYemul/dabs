# This is a Databricks asset bundle definition for dabs.
# See https://docs.databricks.com/dev-tools/bundles/index.html for documentation.
bundle:
  name: dabs
  uuid: e7f4ef9a-cb9b-404a-aec4-cd6c44c31f10

artifacts:
  python_artifact:
    type: whl
    build: uv build --wheel

include:
  - resources/*.yml
  - resources/*/*.yml

targets:
  dev:
    # The default target uses 'mode: development' to create a development copy.
    # - Deployed resources get prefixed with '[dev my_user_name]'
    # - Any job schedules and triggers are paused by default.
    # See also https://docs.databricks.com/dev-tools/bundles/deployment-modes.html.
    mode: development
    default: true
    workspace:
      host: https://adb-731162118893884.4.azuredatabricks.net:443

  prod:
    mode: production
    workspace:
      host: https://adb-731162118893884.4.azuredatabricks.net:443
      # We explicitly deploy to /Workspace/Users/navallyemul@gmail.com to make sure we only have a single copy.
      root_path: /Workspace/Users/navallyemul@gmail.com/.bundle/${bundle.name}/${bundle.target}
    permissions:
      - user_name: navallyemul@gmail.com
        level: CAN_MANAGE
